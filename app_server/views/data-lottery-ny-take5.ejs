<% include header.ejs %>

<section id="content-1" class="container">
	<p>There is a <strong>1:575,757</strong> chance of winning the New York State <a href="http://nylottery.ny.gov/wps/portal/Home/Lottery/Home/Daily+Games/TAKE+5" target="_blank">Take 5</a> lottery jackpot&mdash;if you correctly pick <strong>all five winning numbers</strong> between 1 and 39. Obviously, this tool <em>cannot</em> and <em>does not</em> claim to be able predict winning lottery numbers. Rather, this tool analyzes the Take 5 historical data and helps you pick the first number (the lowest number drawn) and the last number (the highest number drawn).</p>
</section>


<section id="content-2" class="container">
	<div id="drawings-total"><%= drawings.length %></div>


</section>


<section id="content-3" class="container">
	<table class="table table-hover">
		<thead>
			<tr><th>Date</th><th>Day</th><th>1st</th><th>2nd</th><th>3rd</th><th>4th</th><th>5th</th></tr>
		</thead>
		<tbody>
<%
	for(var i = 0; i < drawings.length; i++) {
		drawings[i].dateFormatDate = new Date(drawings[i].draw_date)
		drawings[i].draw_day = drawings[i].dateFormatDate.getDay();
		function getDayString(DayNumber) {
			switch(DayNumber) {
				case 0:
					return 'Monday';
					break;
				case 1:
					return 'Tuesday';
					break;
				case 2:
					return 'Wednesday';
					break;
				case 3:
					return 'Thursday';
					break;
				case 4:
					return 'Friday';
					break;
				case 5:
					return 'Saturday';
					break;
				case 6:
					return 'Sunday';
					break;
			}
		}
%>
			<tr><td><%= drawings[i].draw_date.slice(0,10) %></td><td><%= getDayString(drawings[i].draw_day) %></td><td><%= drawings[i].winning_numbers.slice(0,2) %></td><td><%= drawings[i].winning_numbers.slice(3,5) %></td><td><%= drawings[i].winning_numbers.slice(6,8) %></td><td><%= drawings[i].winning_numbers.slice(9,11) %></td><td><%= drawings[i].winning_numbers.slice(12,14) %></td></tr>
<% } %>
		</tbody>
	</table>
</section>

<% include footer.ejs %>